records.h (the Interface) which declares the functions and data structures; 
records.c (the Implementation) which contains the actual function logic and manages the private data;
main.c (the Control) which contains the menu and the application's starting point.
